export enum DeliveryStatus {
  ASSIGNED = "assigned",
  ACCEPTED = "accepted",
  REJECTED = "rejected",
  PICKED = "picked",
  DELIVERED = "delivered",
  CANCELLED = "cancelled",
  REASSIGNED = "reassigned",
}
